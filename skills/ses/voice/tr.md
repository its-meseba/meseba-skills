# Voice — Turkish

> Stub. Awaiting first sample. Run `../scripts/extract-voice.md` against a real Semih document in Turkish to populate.

## What goes here once populated

Same categories as `voice/en.md`:

- Vocabulary (Turkish-specific tics, words to avoid, register markers like `şey`, `ya`, `hani`).
- Sentence shapes (Turkish word order preferences, particle use — `de/da`, `bile`, `işte`).
- Punctuation and case (Turkish quote conventions `«»` vs `""`, percentage placement `%45` not `45%` in Turkish).
- Discourse markers (`okay`, `hadi`, `tamam`, `şimdi`).
- Argumentation tics in Turkish.
- What to avoid (overly formal Turkish — `gerçekleştirmek` over `yapmak`, etc.).

Until populated: do NOT draft Turkish prose for Semih on this skill alone. Ask him to share a sample doc first.
